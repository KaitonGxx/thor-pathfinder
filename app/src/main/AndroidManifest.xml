<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- A short buzz when a double-press or hold shortcut runs -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Bring a moved app back to the front if a launcher lands on top of it -->
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <!-- Check For Updates asks GitHub for the newest release, only when tapped -->
    <uses-permission android:name="android.permission.INTERNET" />

    <queries>
        <!-- Setup checks for Shizuku, and for Wayfinder, which also claims the Back button -->
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="com.thorwayfinder.app" />
        <!-- Offered first in the keep-running list: they work in the background -->
        <package android:name="de.langerhans.odintools" />
        <package android:name="com.aure.clustertune" />
        <!-- Apps a shortcut can open -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>
        <!-- Home screens, which never get moved between screens -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.SECONDARY_HOME" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pathfinder">

        <!-- Moving Pathfinder between screens must not restart it -->
        <activity
            android:name=".ui.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|density|keyboard|keyboardHidden|navigation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".PathfinderService"
            android:exported="false"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service" />
        </service>

        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:enabled="true"
            android:exported="true"
            android:multiprocess="false"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />
    </application>

</manifest>
